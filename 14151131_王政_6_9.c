#include <stdio.h>
int main()
{
	int i,j,t,max,a[15],x,y=100,m=0,b=14,c;
	c=(m+b)/2;
	printf("请由大到小依次输入15个数:\n");
	for(i=0;i<15;i++)
	{
		printf("第%d个数为:",i+1);
		scanf("%d",&a[i]);
	}
	for(i=0;i<14;i++)
	{
		max=i;
		for(j=i+1;j<15;j++)
		{
			if(a[max]<a[j])
				max=j;
		}
		t=a[i];
		a[i]=a[max];
		a[max]=t;
	}
	printf("该数组为:");
	for(i=0;i<15;i++)
	{
		printf("%d,",a[i]);
	}
	printf("\n");
    printf("请输入要查询的数:");
	scanf("%d",&x);
	if(x==a[14])
	{
		y=15;
	}
	if(x>a[0]||x<a[14])
	{
		printf("无此数\n");
	}
	for(i=1;i<15;i++)
	{
	if(x==a[c])
	{
	y=c+1;
    }
	if(x>a[c])
	{
	b=c;
	c=(m+b)/2;
    }
	if(x<a[c])
	{ 
	m=c;
	c=(m+b)/2;
	} 
    }
    if(y==100)
    {
	printf("无此数\n");
    }
	else
	printf("该数是数组中第%d个元素的值.\n",y);
	return 0;
}
